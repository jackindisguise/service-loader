{
	"name": "service-loader",
	"version": "1.3.2",
	"description": "",
	"main": "./dist/service.js",
	"module": "./dist/service.mjs",
	"types": "./dist/service.d.ts",
	"exports": {
		".": {
			"types": "./dist/service.d.ts",
			"import": "./dist/service.mjs",
			"require": "./dist/service.js"
		}
	},
	"scripts": {
		"clean": "npx rimraf -rf dist",
		"build:esm": "tsc",
		"build:cjs": "npx babel dist/esm --out-dir dist/cjs --plugins @babel/plugin-transform-modules-commonjs --copy-files",
		"build": "npm run build:esm && npm run build:cjs",
		"bundle": "npx tsup src/service.ts --format esm,cjs --dts",
		"build:clean": "npm run clean && npm run build",
		"test:clean": "npm run build:clean && node --test",
		"test": "node --test"
	},
	"keywords": [
		"service",
		"service loader",
		"package",
		"package loader"
	],
	"author": "jackindisguise",
	"license": "ISC",
	"devDependencies": {
		"@babel/cli": "^7.28.6",
		"@babel/core": "^7.29.0",
		"@babel/plugin-transform-modules-commonjs": "^7.28.6",
		"@types/node": "^25.9.1",
		"tsup": "^8.5.1",
		"typescript": "^6.0.3"
	},
	"dependencies": {
		"sw": "github:jackindisguise/sw"
	}
}
